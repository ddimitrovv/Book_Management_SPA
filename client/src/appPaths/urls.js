const baseUrl = 'http://127.0.0.1:8000/'

const urls = {
    Home: baseUrl,
    Login: `${baseUrl}login/`,
    Logout: `${baseUrl}logout/`,
    Books: `${baseUrl}books/`
  };
  
  export default urls;